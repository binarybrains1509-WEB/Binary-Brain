server.port=8080
spring.application.name=binarybrains-backend
server.error.include-message=always
logging.level.com.binarybrains=INFO

# Optional local backend/.env file for development secrets.
spring.config.import=optional:file:.env[.properties]

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:binarybrain1509@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
binarybrains.inquiry-recipient=binarybrain1509@gmail.com
